name: Build QuadrinhosEroticosBlog

on:
  workflow_dispatch:
  push:
    paths:
      - 'src/pt/quadrinhoseroticosblog/**'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK
        uses: actions/setup-java@v5
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v3

      - name: Format Code
        run: ./gradlew :src:pt:quadrinhoseroticosblog:spotlessApply

      - name: Build Extension
        run: ./gradlew :src:pt:quadrinhoseroticosblog:assembleRelease

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: quadrinhoseroticosblog-apk
          path: src/pt/quadrinhoseroticosblog/build/outputs/apk/release/*.apk
