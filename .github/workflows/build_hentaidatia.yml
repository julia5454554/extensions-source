name: Build HentaiDaTia

on:
  workflow_dispatch:
  push:
    paths:
      - 'src/pt/hentaidatia/**'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v3

      - name: Format Code
        run: ./gradlew :src:pt:hentaidatia:spotlessApply

      - name: Build Extension
        run: ./gradlew :src:pt:hentaidatia:assembleRelease

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: hentaidatia-apk
          path: src/pt/hentaidatia/build/outputs/apk/release/*.apk
