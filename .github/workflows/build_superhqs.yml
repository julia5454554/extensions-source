name: Build Super Hqs

on:
  workflow_dispatch:
  push:
    paths:
      - 'src/pt/superhqs/**'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK
        uses: actions/setup-java@v5
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v3

      - name: Format Code
        run: ./gradlew :src:pt:superhqs:spotlessApply

      - name: Build Extension
        run: ./gradlew :src:pt:superhqs:assembleRelease

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: super-hqs-apk
          path: src/pt/superhqs/build/outputs/apk/release/*.apk
