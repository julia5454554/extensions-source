name: Build HentaisBlog

on:
  workflow_dispatch:
  push:
    paths:
      - 'src/pt/hentaisblog/**'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v3

      - name: Format Code
        run: ./gradlew :src:pt:hentaisblog:spotlessApply

      - name: Build Extension
        run: ./gradlew :src:pt:hentaisblog:assembleRelease

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: hentaisblog-apk
          path: src/pt/hentaisblog/build/outputs/apk/release/*.apk
