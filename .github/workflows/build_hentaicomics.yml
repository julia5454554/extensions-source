name: Build HentaiComics Extension

on:
  push:
    branches: [ main ]
    paths:
      - 'src/pt/hentaicomics/**'
      - 'build-logic/**'
      - 'core/**'
      - 'compiler/**'
      - '.github/workflows/hentaicomics.yml'
  pull_request:
    branches: [ main ]
    paths:
      - 'src/pt/hentaicomics/**'
      - 'build-logic/**'
      - 'core/**'
      - 'compiler/**'
      - '.github/workflows/hentaicomics.yml'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: 'gradle'

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Spotless Apply (auto-format)
        run: ./gradlew spotlessApply

      - name: Build only HentaiComics extension
        run: ./gradlew :src:pt:hentaicomics:assembleRelease

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: hentaicomics-extension-apk
          path: src/pt/hentaicomics/build/outputs/apk/release/*.apk
