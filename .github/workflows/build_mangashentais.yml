name: Build MangasHentais

on:
  workflow_dispatch:
  push:
    paths:
      - 'src/pt/mangashentais/**'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v3

      - name: Format Code
        run: ./gradlew :src:pt:mangashentais:spotlessApply

      - name: Build Extension
        run: ./gradlew :src:pt:mangashentais:assembleRelease

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: mangashentais-apk
          path: src/pt/mangashentais/build/outputs/apk/release/*.apk
