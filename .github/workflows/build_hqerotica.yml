name: Build HqErotica

on:
  workflow_dispatch:
  push:
    paths:
      - 'src/pt/hqerotica/**'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK
        uses: actions/setup-java@v5
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v3

      - name: Format Code
        run: ./gradlew :src:pt:hqerotica:spotlessApply

      - name: Build Extension
        run: ./gradlew :src:pt:hqerotica:assembleRelease

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: hqerotica-apk
          path: src/pt/hqerotica/build/outputs/apk/release/*.apk
